# AgenticAI-Bootcamp

# Windows Anaconda prompt, Go to folder the current working directory
        conda create --prefix ./env python=3.12 -y
        conda activate ./env
        pip install -r requirements.txt
        pip install python-dotenv
        python main.py

#

# remove the file from git's index
    git rm --cached path/to/your/file.txt
    git commit -m "Stop tracking and ignore path/to/your/file.txt"
#